<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shui.study.mapper.StudentMapper">
    <select id="findAll" resultType="com.shui.study.domain.Student">
        select id, name,ename,age from student
    </select>
</mapper>